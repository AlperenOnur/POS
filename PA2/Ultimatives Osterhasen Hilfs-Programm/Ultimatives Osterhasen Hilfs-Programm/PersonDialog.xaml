<Window x:Class="Ultimatives_Osterhasen_Hilfs_Programm.PersonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ultimatives_Osterhasen_Hilfs_Programm"
        mc:Ignorable="d"
        Title="PersonDialog" Height="200" Width="400">
    <DockPanel>
       
        <StackPanel DockPanel.Dock="Top">
            <Label>Neue Person hinzufügen:</Label>
            <StackPanel Orientation="Horizontal">
                <Label Width="100" HorizontalContentAlignment="Right">Name:</Label>
                <TextBox Text="{Binding Name}" Width="200"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Width="100" HorizontalContentAlignment="Right">Latitude:</Label>
                <TextBox Text="{Binding Latitude }" Width="200"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Width="100" HorizontalContentAlignment="Right">Longitude:</Label>
                <TextBox Text="{Binding Longitude }" Width="200"></TextBox>
            </StackPanel>


        </StackPanel>
        <Grid Margin="0, 10, 0, 0" DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="OnCancel_Click">Abbrechen</Button>
            <Button Grid.Column="2" Click="OnReg_Click">Erstellen</Button>
        </Grid>


    </DockPanel>
</Window>
